server:
  port: 8081
  servlet:
    context-path: /@project.artifactId@
datasource:
  pgs:
    jdbcUrl: jdbc:postgresql://${DB_URL}:5432/${DB_NAME}
    password: ${DB_PASSWORD}
    username: ${DB_USER}